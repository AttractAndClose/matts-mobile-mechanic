# Add Section to Page

Add a new section component to the homepage. Describe what the section should contain: $ARGUMENTS

Guidelines:
1. Create the component in `src/components/`
2. Use consistent spacing: `py-16 md:py-24` for vertical padding
3. Use container pattern: `container mx-auto px-6 md:px-12`
4. Follow the brand color palette defined in `src/styles/global.css`
5. Alternate background colors between sections for visual separation
6. Make sections responsive with mobile-first design
7. Import and add the component to `src/pages/index.astro`
